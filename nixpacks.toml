[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "SECRET_KEY=build-phase-dummy-key python manage.py collectstatic --noinput"
]

[start]
cmd = "python manage.py migrate --noinput && gunicorn ielts_platform.wsgi:application --bind 0.0.0.0:8000 --workers 2 --timeout 120"
